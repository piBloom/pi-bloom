{ ... }:

{
  # The current first-boot module exposes commands and policy, not a dedicated
  # nixpi.firstboot option namespace. Keep this file as the co-located landing
  # spot if firstboot-specific options are added later.
}
