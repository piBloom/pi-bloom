# network.conf — Calamares network module configuration for Bloom OS
# Enables WiFi network selection during installation
---
# Use NetworkManager backend
backend: networkmanager

# Show the page even if there's an internet connection
# (user may want to switch to a different network)
allowEmpty: false

# Require internet connection to proceed
# Set to false to allow offline installation
requireWifi: false
